# Optional requirements for advanced signal processing
# These require Fortran compiler (gfortran)
# Install with: brew install gcc && pip install -r requirements-optional.txt

scipy>=1.14.0
librosa>=0.10.2
pywavelets>=1.7.0
opacus>=1.5.2
